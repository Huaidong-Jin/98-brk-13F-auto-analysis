"use client";

import { HoldingsTable } from "@/components/HoldingsTable";

/**
 * Alias wrapper around HoldingsTable to make the intent in Analyst Mode
 * clearer and keep a stable import path under components/tables.
 */
export const HoldingsExplorerTable = HoldingsTable;

