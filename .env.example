# Database (SQLite for dev; Postgres for prod)
DATABASE_URL=sqlite+aiosqlite:///./data/brk13f.db

# SEC EDGAR (required: name + email for User-Agent)
SEC_USER_AGENT=Jimmy jimmyandone@gmail.com

# Google Drive (production: Service Account JSON path or content)
GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON=
GOOGLE_DRIVE_FOLDER_ID=

# Alerts (Slack preferred; email fallback)
SLACK_WEBHOOK_URL=
ALERT_EMAIL=
# SMTP_* if using email alerts

# Optional: override for local dev
# ENV=development
